// CONTRATO:
//   input:
//     x: number
//   output: number
function suma(x: number, y: number): number {
    return x + y;
}

/**
 * CONTRATO:
 *   input:
 *     x: number
 *   output: number
 */
function resta(x: number, y: number): number {
    return x - y;
}
